"use client";
import { useRouter } from "next/navigation";
import { ArrowLeft, CheckCircle2 } from "lucide-react";
import { useState } from "react";
import { AuthShell } from "@/components/auth/auth-shell";
export default function ForgotPassword(){const router=useRouter();const[step,setStep]=useState(0);return <AuthShell eyebrow="Password recovery" title="Reset your password" description="Verify ownership using a one-time code, then choose a password that meets the security policy."><div className="space-y-4">{step===0&&<><label><span className="mp-label">Registered phone number</span><input className="mp-input" defaultValue="+254 712 345 678"/></label><p className="text-xs text-slate-500">Reset codes are rate-limited to protect your account.</p></>}{step===1&&<><p className="text-sm text-slate-600">Enter the six-digit OTP sent to your registered phone.</p><input className="mp-input text-center font-mono text-xl tracking-[.45em]" defaultValue="528194" maxLength={6}/></>}{step===2&&<><label><span className="mp-label">New password</span><input type="password" className="mp-input" defaultValue="NewMarketPay#27"/></label><label><span className="mp-label">Confirm new password</span><input type="password" className="mp-input" defaultValue="NewMarketPay#27"/></label><div className="rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-xs text-emerald-700"><CheckCircle2 className="mr-2 inline h-4 w-4"/>All existing sessions will be invalidated after a successful reset.</div></>}</div><div className="mt-6 flex justify-between"><button onClick={()=>step===0?router.push('/login'):setStep(s=>s-1)} className="mp-btn-secondary"><ArrowLeft className="h-4 w-4"/>Back</button><button onClick={()=>step<2?setStep(s=>s+1):router.push('/login')} className="mp-btn-primary">{step===2?'Reset password':'Continue'}</button></div></AuthShell>}
