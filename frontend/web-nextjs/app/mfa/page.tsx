"use client";
import { useRouter } from "next/navigation";
import { KeyRound, Mail, MessageSquareText, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { AuthShell } from "@/components/auth/auth-shell";
export default function MfaPage(){const router=useRouter();const[method,setMethod]=useState('SMS OTP');return <AuthShell eyebrow="Multi-factor authentication" title="Verify it’s really you" description="Retailer, wholesaler and administrator roles require a second factor after a correct password."><div className="grid grid-cols-3 gap-2">{[["SMS OTP",MessageSquareText],["TOTP",KeyRound],["Email OTP",Mail]].map(([name,Icon]:any)=><button key={name} onClick={()=>setMethod(name)} className={`rounded-xl border p-3 text-left ${method===name?'border-brand-400 bg-brand-50':'border-slate-200'}`}><Icon className="h-4 w-4 text-brand-600"/><p className="mt-2 text-xs font-bold">{name}</p></button>)}</div><div className="mt-5 rounded-xl bg-slate-50 p-4"><div className="flex items-center gap-2"><ShieldCheck className="h-4 w-4 text-emerald-600"/><p className="text-sm font-bold">{method} challenge issued</p></div><p className="mt-1 text-xs text-slate-500">Complete this challenge within 3 minutes.</p><input className="mp-input mt-4 text-center font-mono text-xl tracking-[.4em]" maxLength={6} defaultValue="481206"/></div><button onClick={()=>router.push('/vendor')} className="mp-btn-primary mt-5 w-full justify-center">Verify and continue</button><button onClick={()=>router.push('/login')} className="mt-3 w-full text-center text-sm font-semibold text-slate-500">Cancel sign in</button></AuthShell>}
